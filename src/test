
CXX=g++
MPICXX=mpic++

EXE=squirrels

SRC=squirrel_actor.c
OBJ=$(SRC:.c=.o)

PROVIDED_DIR=../provided/
PROVIDED_OBJ=ran2.o squirrel-functions.o

all: ran2.o
	@echo hello
#$(MPICXX) -o $(EXE) -I$(PROVIDED_DIR) $(PROVIDED_OBJ) $(OBJ) $(EXE).cpp

clean:
	rm -f *.o
	rm -f squirrels

%.o:
	@echo $@ $<

.c.o:
	@echo $@ $<
	#$(CXX) -c -o $@ -I$(PROVIDED_DIR) $(PROVIDED_OBJ) $<

provided:
	#$(CXX) -c -o ran2.o $(PROVIDED_DIR)ran2.c
	#$(CXX) -c -o squirrel-functions.o $(PROVIDED_DIR)squirrel-functions.c
#$(CXX) -c -o $@ $(PROVIDED_DIR)$(@:.o=.c)
